@{

    var lyout = HttpContext.Current.Request.RequestContext.RouteData.Values["Controller"].ToString();
 
    string layout = "";
    if (lyout=="Edit"||lyout=="Create")
    {
        layout = "~/Views/Shared/_students.cshtml";
    }
    else
    {
        layout = "~/Views/Shared/_Layout.cshtml";
    }

    Layout = layout;
}
